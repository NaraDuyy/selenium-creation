# CloakBrowser license key -- unlocks the newest browser build (Chromium 151).
# Get a free one at https://cloakbrowser.dev/free (GitHub sign-in), paste it
# after the = sign, save. Leave it empty to keep using the keyless 146 build.
#
# This file is gitignored: the key stays on this PC and is never pushed.
CLOAKBROWSER_LICENSE_KEY=
